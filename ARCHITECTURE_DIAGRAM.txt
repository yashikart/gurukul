╔══════════════════════════════════════════════════════════════════════════════╗
║                    GURUKUL CHATBOT ARCHITECTURE                               ║
╚══════════════════════════════════════════════════════════════════════════════╝

CURRENT (BROKEN) SETUP:
═══════════════════════════════════════════════════════════════════════════════

  ┌─────────────────────────────────────┐
  │   Frontend (localhost:5173)         │
  │   React App                         │
  │   VITE_CHAT_API_BASE_URL=           │
  │   https://69f21cfd502a.ngrok...     │
  └─────────────────────────────────────┘
                  │
                  │ HTTPS Request
                  │ Origin: http://localhost:5173
                  ↓
  ┌─────────────────────────────────────┐
  │   Ngrok Tunnel                      │
  │   https://69f21cfd502a.ngrok...     │
  │   Forwards to: localhost:8000 ❌    │  ← WRONG PORT!
  └─────────────────────────────────────┘
                  │
                  │ HTTP
                  ↓
  ┌─────────────────────────────────────┐
  │   Gateway Service (8000)            │  ← Service not running
  │   ❌ Connection Refused              │     or not needed
  └─────────────────────────────────────┘

  Result: ERR_NGROK_8012 - Connection refused


CORRECT (FIXED) SETUP:
═══════════════════════════════════════════════════════════════════════════════

  ┌─────────────────────────────────────┐
  │   Frontend (localhost:5173)         │
  │   React App                         │
  │   VITE_CHAT_API_BASE_URL=           │
  │   https://YOUR-NEW-URL.ngrok...     │
  └─────────────────────────────────────┘
                  │
                  │ HTTPS Request
                  │ Origin: http://localhost:5173
                  ↓
  ┌─────────────────────────────────────┐
  │   Ngrok Tunnel                      │
  │   https://YOUR-NEW-URL.ngrok...     │
  │   Forwards to: localhost:8001 ✅    │  ← CORRECT PORT!
  └─────────────────────────────────────┘
                  │
                  │ HTTP
                  ↓
  ┌─────────────────────────────────────┐
  │   Chatbot Service (8001) ✅         │
  │   FastAPI + CORS Middleware         │
  │   - Explicit OPTIONS handlers       │
  │   - CORSMiddleware configured       │
  │   - Request logging enabled         │
  └─────────────────────────────────────┘
                  │
                  ↓
  ┌─────────────────────────────────────┐
  │   Backend Services                  │
  │   - MongoDB (chat history)          │
  │   - LLM APIs (Groq, OpenAI)         │
  │   - TTS Service (optional)          │
  └─────────────────────────────────────┘

  Result: ✅ CORS headers present, requests succeed


CORS PREFLIGHT FLOW (OPTIONS Request):
═══════════════════════════════════════════════════════════════════════════════

  Browser                    Ngrok                   Chatbot Service
    │                          │                           │
    │ OPTIONS /chatpost        │                           │
    │ Origin: localhost:5173   │                           │
    ├─────────────────────────>│                           │
    │                          │ OPTIONS /chatpost         │
    │                          │ Origin: localhost:5173    │
    │                          ├──────────────────────────>│
    │                          │                           │
    │                          │                    ┌──────┴──────┐
    │                          │                    │ 1. Log req  │
    │                          │                    │ 2. OPTIONS  │
    │                          │                    │    handler  │
    │                          │                    │ 3. CORS     │
    │                          │                    │    middleware│
    │                          │                    └──────┬──────┘
    │                          │                           │
    │                          │ 200 OK                    │
    │                          │ Access-Control-Allow-     │
    │                          │   Origin: localhost:5173  │
    │                          │ Access-Control-Allow-     │
    │                          │   Methods: GET,POST,...   │
    │                          │<──────────────────────────┤
    │ 200 OK                   │                           │
    │ (CORS headers)           │                           │
    │<─────────────────────────┤                           │
    │                          │                           │
    │ Now send actual POST     │                           │
    │ /chatpost                │                           │
    ├─────────────────────────>│──────────────────────────>│
    │                          │                           │
    │<─────────────────────────┤<──────────────────────────┤
    │ Response with CORS       │                           │
    │ headers                  │                           │


CONFIGURATION FILES:
═══════════════════════════════════════════════════════════════════════════════

  Backend/.env
  ┌─────────────────────────────────────────────────────────────┐
  │ ALLOWED_ORIGINS=http://localhost:5173,...,https://YOUR-URL  │
  │ ALLOW_ORIGIN_REGEX=https://.*\.ngrok-free\.app              │
  │ NGROK_URL=https://YOUR-URL.ngrok-free.app                   │
  └─────────────────────────────────────────────────────────────┘
                  │
                  │ Loaded by shared_config
                  ↓
  Backend/common/cors.py
  ┌─────────────────────────────────────────────────────────────┐
  │ def configure_cors(app):                                     │
  │   origins = os.getenv('ALLOWED_ORIGINS').split(',')         │
  │   allow_regex = os.getenv('ALLOW_ORIGIN_REGEX')             │
  │   app.add_middleware(CORSMiddleware, ...)                   │
  └─────────────────────────────────────────────────────────────┘
                  │
                  │ Called by
                  ↓
  Backend/dedicated_chatbot_service/chatbot_api.py
  ┌─────────────────────────────────────────────────────────────┐
  │ from common.cors import configure_cors                       │
  │ app = FastAPI(...)                                          │
  │ configure_cors(app)  # Apply CORS middleware                │
  │                                                              │
  │ @app.options("/chatpost")                                   │
  │ async def chatpost_preflight():                             │
  │     return Response(status_code=200)                        │
  └─────────────────────────────────────────────────────────────┘

  new frontend/.env.local
  ┌─────────────────────────────────────────────────────────────┐
  │ VITE_CHAT_API_BASE_URL=https://YOUR-URL.ngrok-free.app      │
  │ VITE_CHATBOT_API_URL=https://YOUR-URL.ngrok-free.app        │
  └─────────────────────────────────────────────────────────────┘
                  │
                  │ Loaded by Vite
                  ↓
  new frontend/src/config.js
  ┌─────────────────────────────────────────────────────────────┐
  │ export const CHAT_API_BASE_URL =                            │
  │   import.meta.env.VITE_CHAT_API_BASE_URL                    │
  └─────────────────────────────────────────────────────────────┘
                  │
                  │ Used by
                  ↓
  new frontend/src/pages/Chatbot.jsx
  ┌─────────────────────────────────────────────────────────────┐
  │ import { CHAT_API_BASE_URL } from "../config"               │
  │ fetch(`${CHAT_API_BASE_URL}/chatpost?user_id=${userId}`)    │
  └─────────────────────────────────────────────────────────────┘


PORT MAPPING:
═══════════════════════════════════════════════════════════════════════════════

  Port    Service                  Status      Ngrok?
  ────────────────────────────────────────────────────────────────
  5173    Frontend (React)         Running     No
  8000    Gateway Service          Optional    ❌ NO (wrong port)
  8001    Chatbot Service          Running     ✅ YES (correct!)
  8002    Financial Service        Optional    No
  8007    TTS Service              Optional    No


STARTUP SEQUENCE:
═══════════════════════════════════════════════════════════════════════════════

  1. Start Chatbot Service
     cd Backend\dedicated_chatbot_service
     python chatbot_api.py
     → Listens on port 8001
     → Loads CORS config from Backend/.env
     → Logs: "🔒 CORS CONFIGURATION"

  2. Start Ngrok Tunnel
     ngrok http 8001 --region=in --host-header=localhost
     → Forwards to localhost:8001
     → Provides public HTTPS URL

  3. Update Configuration
     → Backend/.env: Add ngrok URL to ALLOWED_ORIGINS
     → new frontend/.env.local: Set VITE_CHAT_API_BASE_URL

  4. Restart Chatbot Service
     → Reload .env with new ngrok URL
     → CORS now allows requests from ngrok URL

  5. Clear Ngrok Warning
     → Visit ngrok URL in browser
     → Click "Visit Site"

  6. Start Frontend
     cd new frontend
     npm run dev
     → Loads ngrok URL from .env.local
     → Sends requests to ngrok URL


VERIFICATION:
═══════════════════════════════════════════════════════════════════════════════

  ✓ Check ngrok is forwarding to 8001:
    → Look at ngrok dashboard: "Forwarding ... -> http://localhost:8001"

  ✓ Check chatbot service is running:
    → netstat -ano | findstr :8001

  ✓ Check CORS configuration:
    → Look for "🔒 CORS CONFIGURATION" in chatbot service logs

  ✓ Test CORS preflight:
    → curl -i -X OPTIONS "https://YOUR-URL/chatpost" \
        -H "Origin: http://localhost:5173" \
        -H "Access-Control-Request-Method: POST"
    → Should return: Access-Control-Allow-Origin: http://localhost:5173

  ✓ Test from browser:
    → Open http://localhost:5173/chatbot
    → Send a message
    → Check Network tab: No CORS errors


KEY TAKEAWAYS:
═══════════════════════════════════════════════════════════════════════════════

  1. Ngrok MUST forward to PORT 8001 (chatbot service)
  2. Update ngrok URL in BOTH Backend/.env AND frontend/.env.local
  3. Restart chatbot service after updating .env
  4. Clear ngrok warning page by visiting in browser
  5. CORS is configured centrally via Backend/common/cors.py
  6. Explicit OPTIONS handlers ensure preflight requests work
  7. Request logging helps debug CORS issues

═══════════════════════════════════════════════════════════════════════════════
